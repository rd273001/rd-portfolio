# 3D phone model

`android-phone.glb` is an original Pixel-style Android device built for this portfolio (Obsidian finish, camera bar, punch-hole). It is not an Apple or Google product file.

Regenerate:

```bash
npm run build:phone-model
```